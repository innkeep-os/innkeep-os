# pile/

Drop anything here — any file type, no structure, no naming. Text scraps, whole
drafted documents, screenshots, downloads.

Processing (`/process-pile`) means moving the file whole if it **is** the
artefact, or dissolving its content into the right files and deleting the
emptied capture.

The point is that capture should cost nothing. Filing is a separate job.
