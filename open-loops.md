# Open loops

<!-- The session handoff ledger. Claude: read this at session start; before
ending a session, add anything you are leaving in flight and DELETE lines you
closed. One line per loop: date [area] what is dangling → next: the step.
Empty = all clear, and empty is the goal state. -->

_Nothing is dangling. That is the goal state, not an empty file waiting to be
filled._
